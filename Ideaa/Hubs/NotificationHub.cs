using Microsoft.AspNetCore.SignalR;

namespace Ideaa.HubS
{
    public class NotificationHub : Hub
    {
        public async Task SendNotification(string message)
        {
            await Clients.All.SendAsync("ReceiveNotification", message);
        }
    }
}
